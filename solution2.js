// declare function to generate an arry
function generateAnArry(start,end){

// declare a new arry where we are going to store our generated numbers
    let nad = []

// we use a for lop to itterate through the array adding new numbers
for(let i=start;i <= end; i++ ){
    nad.push(i)
}
 console.log (nad)
}
generateAnArry(start,end)